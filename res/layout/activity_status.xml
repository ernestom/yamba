<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="fill_vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".StatusActivity" >

    <EditText
        android:id="@+id/status_text_status"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/status_button_status"
        android:layout_marginTop="18dp"
        android:ems="10"
        android:hint="@string/status_hint_status"
        android:inputType="textMultiLine"
        android:text="@string/status_hint_status" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/status_button_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/status_text_status"
        android:layout_marginRight="14dp"
        android:layout_marginTop="21dp"
        android:text="@string/status_button_status" />

</RelativeLayout>